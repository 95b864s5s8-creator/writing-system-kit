# 05 · 安装指南（各平台）

> 把整个 `writing-system-kit/` 文件夹复制到目标 agent 的 skill 目录即可。
> 每个平台只需两步：复制文件夹 → 告诉 agent 有这套系统。

---

## WorkBuddy（本机已装部分组件）

```bash
# 用户级 skill 目录
cp -R writing-system-kit ~/.workbuddy/skills/
```

装完后在对话里直接说："我想写关于XX" / "生成卡片" / "结构化" / "成稿"，系统会自动触发。

注意：WorkBuddy 上已有独立的 vibe-writing skill（四阶段细节在 `~/.workbuddy/skills/vibe-writing/`），本包是它的便携通用版，两者不冲突。

## Claude Code

```bash
# 用户级（所有项目可用）
cp -R writing-system-kit ~/.claude/skills/

# 或项目级（单个项目）
cp -R writing-system-kit <project>/.claude/skills/
```

Claude Code 会在会话开始自动加载 skill 目录下的 SKILL.md。装完直接说"我想写关于XX"。

## Codex CLI

```bash
# 用户级
cp -R writing-system-kit ~/.codex/skills/

# 或项目级
cp -R writing-system-kit <project>/.agents/skills/
```

## 其他 AI 客户端（不支持 skill 目录的）

把 `SKILL.md` + `01-04` 四个文档打包成一个文件（或直接复制这 5 个文件），粘贴到对话开头，然后说：

```
请读取这套写作系统（人写AI辅：四阶段流程 + 风格档案 + 七技巧方法论 + 去AI味规则）。
接下来我们按这套系统协作写作：我主导选题和判断，你负责激发、结构方案、润色和成稿。
```

---

## 换电脑/换 agent 的标准流程

1. 从 GitHub 私有仓库 clone 或下载本包
2. 按上面对应平台的路径复制
3. 开始对话，说"我想写关于XX"测试触发

## 目录说明

```
writing-system-kit/
├── SKILL.md                 总入口（frontmatter + 骨架 + 命令）
├── 01_核心流程_vibe-writing.md  四阶段完整规则
├── 02_写作风格档案.md       写作偏好（自然段落/判断表达/反模式）
├── 03_七技巧方法论.md        信息价值+情绪价值落实到每段
├── 04_去AI味规则.md          AI 腔清单 + 检查表
└── 05_安装指南.md            本文件
```

## 维护

- 这是"人写AI辅"系统的便携版本，作者（不观）维护
- 更新：改完文件后 git add/commit/push，各设备 git pull
- 建议：如果某次写作中作者给了新的风格反馈或新的反模式，及时补充进 02 或 04
